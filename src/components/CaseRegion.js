import React from 'react';
import PropTypes from 'prop-types';

const CaseRegion = props => {
    return (
        <div>

        </div>
    );
};

CaseRegion.propTypes = {

};

export default CaseRegion;