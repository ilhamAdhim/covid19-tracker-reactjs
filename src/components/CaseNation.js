import React from 'react';
import PropTypes from 'prop-types';

const CaseNation = props => {
    return (
        <div>

        </div>
    );
};

CaseNation.propTypes = {

};

export default CaseNation;